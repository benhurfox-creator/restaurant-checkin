# 餐厅打卡本

一个可以记录打卡餐厅并上传菜单图片的本地浏览器应用。

## 功能

- 添加餐厅名称、菜系、城市、打卡日期、评分和备注
- 为每家餐厅上传多张菜单图片
- 搜索、排序、编辑和删除餐厅记录
- 数据保存在浏览器本地

## 本地预览

```bash
python3 -m http.server 5173
```

打开 `http://127.0.0.1:5173/`。

## Render 部署

这是一个静态站点。Render 可以直接使用 `render.yaml` 创建 Static Site。
